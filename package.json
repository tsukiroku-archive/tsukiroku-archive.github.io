{
  "name": "web",
  "private": true,
  "version": "1.0.0",
  "homepage": "https://tsukiroku.github.io",
  "scripts": {
    "build": "tsc && vite build",
    "preview": "yarn build && vite preview",
    "deploy": "yarn build && gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^3.2.3",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@vitejs/plugin-react": "^1.3.0",
    "typescript": "^4.6.3",
    "vite": "^2.9.5"
  }
}
